import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 40,
    padding: 10,
    height: "100%",
  },
  textInput: {
    padding: 10,
    height: 50,
    backgroundColor: "#eee",
    marginVertical: 5,
  },
});

export default styles;
